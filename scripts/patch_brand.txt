            <div className="pt-8 mt-4 border-t border-stone-200">
              <label className="inline-flex items-center gap-3 cursor-pointer mb-6">
                <input type="checkbox" checked={isBrand} onChange={e => setIsBrand(e.target.checked)} className="w-6 h-6 rounded border-stone-300 text-stone-900 focus:ring-stone-900 transition-all cursor-pointer" />
                <span className="text-lg font-bold text-stone-900 flex items-center gap-2">
                  <BadgeCheck className="w-5 h-5 text-indigo-500" /> Đánh dấu là Nhạc Thương Hiệu
                </span>
              </label>

              {isBrand && (
                <div className="space-y-6 bg-indigo-50/50 p-6 rounded-2xl border border-indigo-100 mb-6">
                  <div>
                    <label className="block text-sm font-bold text-stone-700 mb-2">Tên đối tác (Thương hiệu) *</label>
                    <input type="text" value={brandName} onChange={e => setBrandName(e.target.value)} required={isBrand} placeholder="VD: Vingroup, Viettel..." className="w-full border border-stone-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-white" />
                  </div>
                  <div>
                    <label className="block text-sm font-bold text-stone-700 mb-2">Logo đối tác (Vuông/Tròn, PNG/JPG) *</label>
                    {uploadedBrandLogoUrl ? (
                      <div className="flex items-center gap-4 bg-white border border-stone-200 p-3 rounded-xl shadow-sm">
                        <img src={uploadedBrandLogoUrl} alt="Logo" className="w-12 h-12 object-contain rounded-lg" />
                        <div className="flex-1 min-w-0">
                          <p className="text-sm font-bold text-stone-900 truncate">Logo đã tải lên</p>
                          <a href={uploadedBrandLogoUrl} target="_blank" rel="noreferrer" className="text-xs text-indigo-600 hover:underline truncate block">Xem ảnh</a>
                        </div>
                        <button type="button" onClick={() => setUploadedBrandLogoUrl('')} className="p-2 text-stone-400 hover:text-red-500 hover:bg-red-50 rounded-lg transition-colors">
                          <X className="w-5 h-5" />
                        </button>
                      </div>
                    ) : (
                      <div className="border-2 border-dashed border-stone-300 rounded-xl p-6 text-center hover:bg-white hover:border-indigo-400 transition-colors relative cursor-pointer group">
                        <input type="file" accept="image/*" onChange={async (e) => {
                          const file = e.target.files?.[0];
                          if (!file) return;
                          setBrandLogoUploadProgress(1);
                          try {
                            const formData = new FormData(); formData.append('file', file);
                            const res = await fetch('/api/upload', { method: 'POST', headers: { 'Authorization': `Bearer ${getAdminToken() || ''}` }, body: formData });
                            if (res.ok) { const data = await res.json(); setUploadedBrandLogoUrl(data.url); }
                          } catch (err) { alert('Lỗi tải lên logo'); }
                          setBrandLogoUploadProgress(0);
                        }} className="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10" />
                        {brandLogoUploadProgress > 0 ? (
                          <div className="flex flex-col items-center gap-2">
                            <div className="w-6 h-6 border-2 border-indigo-500 border-t-transparent rounded-full animate-spin"></div>
                            <p className="text-sm font-medium text-indigo-600">Đang tải lên...</p>
                          </div>
                        ) : (
                          <div className="flex flex-col items-center gap-2">
                            <div className="w-10 h-10 bg-indigo-50 text-indigo-500 rounded-full flex items-center justify-center group-hover:scale-110 transition-transform"><Sparkles className="w-5 h-5" /></div>
                            <p className="text-sm font-bold text-stone-700">Kéo thả hoặc click để chọn ảnh Logo</p>
                          </div>
                        )}
                      </div>
                    )}
                  </div>
                  <div>
                    <label className="block text-sm font-bold text-stone-700 mb-2">Brief khách hàng (nếu có)</label>
                    <textarea value={brandBrief} onChange={e => setBrandBrief(e.target.value)} placeholder="Nhập nội dung brief..." rows={3} className="w-full border border-stone-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-white" />
                  </div>
                  <div>
                    <div className="flex items-center justify-between mb-2">
                      <label className="block text-sm font-bold text-stone-700">Video Tham Khảo (YouTube) - Tối đa 5 video</label>
                      {brandReferenceVideos.length < 5 && (
                        <button type="button" onClick={() => setBrandReferenceVideos([...brandReferenceVideos, ''])} className="text-xs font-bold text-indigo-600 bg-indigo-50 px-2 py-1 rounded-lg hover:bg-indigo-100 flex items-center gap-1"><Plus className="w-3 h-3"/> Thêm video</button>
                      )}
                    </div>
                    <div className="space-y-3">
                      {brandReferenceVideos.map((vid, idx) => (
                        <div key={idx} className="flex items-center gap-2">
                          <input type="url" value={vid} onChange={e => {
                            const newVids = [...brandReferenceVideos]; newVids[idx] = e.target.value; setBrandReferenceVideos(newVids);
                          }} placeholder="https://youtube.com/watch?v=..." className="flex-1 border border-stone-300 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-white" />
                          <button type="button" onClick={() => {
                            setBrandReferenceVideos(brandReferenceVideos.filter((_, i) => i !== idx));
                          }} className="p-2 text-stone-400 hover:text-red-500 hover:bg-red-50 rounded-lg">
                            <X className="w-4 h-4" />
                          </button>
                        </div>
                      ))}
                      {brandReferenceVideos.length === 0 && <p className="text-sm text-stone-500 italic">Chưa có video tham khảo nào.</p>}
                    </div>
                  </div>
                </div>
              )}
            </div>
